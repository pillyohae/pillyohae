package com.example.main.product.repository;

import com.example.common.product.entity.PersonaMessage;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PersonaMessageRepository extends JpaRepository<PersonaMessage, Long> {
}

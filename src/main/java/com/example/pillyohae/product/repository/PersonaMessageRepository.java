package com.example.pillyohae.product.repository;

import com.example.pillyohae.product.entity.PersonaMessage;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PersonaMessageRepository extends JpaRepository<PersonaMessage, Long> {
}
